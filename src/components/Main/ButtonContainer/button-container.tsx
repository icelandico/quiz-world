import React from "react"
import { StartButton } from "./button-container-styles"

const ButtonContainer: React.FC = () => {
  return (
    <div>
      <StartButton>Start the Quiz</StartButton>
    </div>
  )
}

export default ButtonContainer
